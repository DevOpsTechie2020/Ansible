- name: Nginx installation and running
  hosts: frontend
  tasks:
  - name: install Nginx
    ansible.builtin.package:
      name: nginx
      state: present
  - name: Run Nginx
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes